{
  "description": "Chrome extension that redirects domains based on declarative config",
  "devDependencies": {
    "@rivavolt/ext-icons": "^0.1.0",
    "@types/chrome": "^0.0.277",
    "typescript": "^5.9.3",
    "wxt": "^0.20.13"
  },
  "name": "redirect-domains",
  "scripts": {
    "build": "wxt build",
    "dev": "wxt",
    "icons": "ext-icons --letter r --out public/icon",
    "postinstall": "wxt prepare"
  },
  "type": "module",
  "version": "0.2.1"
}
